// TODO: `else`
// TODO: WHEN AS EXPRESSION!
block main(input: many) => (out: int(signal-blue)) {
    let i: var(signal-green);
    when input[signal-red] > 5 {
        i <- 10;
    }
    out <- i;
}

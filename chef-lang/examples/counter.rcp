block counter(lim: int) => (count: int) {
    let driver: int = 1;

    // Drive the counter with a constant combinator
    count <~ driver;

    // Gate expression
    count <- ? count < lim <- count;
}

block main() => (out: int(tank)) {
    let c = counter(5);
    out <- c;
}

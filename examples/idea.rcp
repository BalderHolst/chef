// This will only exist in the blueprint if used
block calculate(a: int, b: int, c: int) -> (d: CYAN) {
    d = a + b * c - 500;
}

// This will always exist in the blueprint
block main() -> () {
    counter c(0..100): any; // Counter that counts once per frame (60hz) and resets when it gets to 100.
    let i;                  // Always zero initialized
    const increment = 4     // Constant valued are just a simple macro
    
    when c == i {
        i += 1;
    }


}
